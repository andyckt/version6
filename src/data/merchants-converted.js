/**
 * Converted Merchants Data for Database Import
 * 
 * This file is generated by the convert-merchants.js script.
 * DO NOT EDIT MANUALLY.
 */

// Profile interface types
const ProfileInterface = {
  SINGLE_SHOP_RESTAURANT: 'single-shop-restaurant',
  MULTIPLE_BRANCH_MERCHANT: 'multiple-branch-merchant',
  ATTRACTION_MERCHANT: 'attraction-merchant',
  HOTEL_MERCHANT: 'hotel-merchant',
  BAR_CLUB_MERCHANT: 'bar-club-merchant',
  STREET_MERCHANT: 'street-merchant',
  BUILDING_MERCHANT: 'building-merchant'
};

// Sample merchant (will be replaced by the converter)
const merchants = [];

// Helper functions to check merchant types
function isSingleLocationMerchant(merchant) {
  return merchant.profileInterface === ProfileInterface.SINGLE_SHOP_RESTAURANT;
}

function isMultiLocationMerchant(merchant) {
  return merchant.profileInterface === ProfileInterface.MULTIPLE_BRANCH_MERCHANT;
}

function isAttractionMerchant(merchant) {
  return merchant.profileInterface === ProfileInterface.ATTRACTION_MERCHANT;
}

function isHotelMerchant(merchant) {
  return merchant.profileInterface === ProfileInterface.HOTEL_MERCHANT;
}

function isBarClubMerchant(merchant) {
  return merchant.profileInterface === ProfileInterface.BAR_CLUB_MERCHANT;
}

function isStreetMerchant(merchant) {
  return merchant.profileInterface === ProfileInterface.STREET_MERCHANT;
}

function isBuildingMerchant(merchant) {
  return merchant.profileInterface === ProfileInterface.BUILDING_MERCHANT;
}

// Export for use in import scripts
module.exports = {
  ProfileInterface,
  merchants,
  isSingleLocationMerchant,
  isMultiLocationMerchant,
  isAttractionMerchant,
  isHotelMerchant,
  isBarClubMerchant,
  isStreetMerchant,
  isBuildingMerchant
}; 